ALTER TABLE save_run ADD COLUMN data_schema_version INTEGER NOT NULL DEFAULT 1;

ALTER TABLE event_log ADD COLUMN event_schema_version INTEGER NOT NULL DEFAULT 1;

CREATE TABLE state_snapshot (
    run_id TEXT NOT NULL PRIMARY KEY,
    sequence INTEGER NOT NULL,
    state_schema_version INTEGER NOT NULL,
    state_json TEXT NOT NULL,
    FOREIGN KEY (run_id) REFERENCES save_run(run_id) ON DELETE CASCADE
);
